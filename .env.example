# Model
MODEL_PATH=model/model.pkl
MODEL_VERSION=1.0.0
MODEL_SOURCE=local

# GCS (if MODEL_SOURCE=gcs)
GCS_BUCKET=medicaidguard-models
GCS_MODEL_KEY=models/v1.0.0/model.pkl

# Server
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=info

# Inference
FRAUD_THRESHOLD=0.5
BATCH_MAX_SIZE=100
SHAP_ENABLED=true
